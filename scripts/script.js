const showMenu = () => {
  const slide = document.getElementById("slide-menu");
  slide.classList.add("show");
};

const closeMenu = () => {
  const slide = document.getElementById("slide-menu");
  slide.classList.remove("show");
};
